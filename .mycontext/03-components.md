# Components Manifest - Living Brain Dashboard

> Auto-generated component specifications with props and sample data.
> Total: 10 core components identified.

## Overview

| Component | Type | Props | Actions | Used In |
|-----------|------|-------|---------|---------|
| `BrainDashboardHeader` | layout | 3 | 1 | `DashboardOverview` |
| `StreamFeedCard` | display | 1 | 0 | `DashboardOverview`, `StreamOfConsciousness` |
| `AgentActivityItem` | display | 1 | 1 | `StreamOfConsciousness` |
| `InteractionHUD` | interactive | 1 | 1 | `DashboardOverview`, `StreamOfConsciousness` |
| `ArtifactTabs` | navigation | 1 | 1 | `DashboardOverview`, `ArtifactBrowser` |
| `CodePreview` | display | 2 | 0 | `ArtifactBrowser` |
| `MarkdownRenderer` | display | 1 | 0 | `ArtifactBrowser` |
| `TaskCardStack` | display | 1 | 2 | `TaskManager` |
| `StatusBadge` | display | 1 | 0 | (Multiple) |
| `ControlButtons` | interactive | 2 | 2 | `DashboardOverview`, `AgentController` |

---

## Component Specifications

### Layout Components

#### `BrainDashboardHeader`
**Description**: The top-level header for the dashboard, displaying the current version, overarching narrative, and global status.
**Used in**: `DashboardOverview`
**Props**:
```typescript
interface BrainDashboardHeaderProps {
  version: string; // Dashboard version
  narrative: string; // The high-level goal
  status: BrainStatus; // 'thinking' | 'idle' | 'error' | etc.
}
```

---

### Display Components

#### `StreamFeedCard`
**Description**: A scrollable container for live updates from agents.
**Used in**: `DashboardOverview`, `StreamOfConsciousness`
**Props**:
```typescript
interface StreamFeedCardProps {
  updates: BrainUpdate[]; // List of historical updates
}
```

#### `AgentActivityItem`
**Description**: An individual unit of activity in the stream (thought, action, or error).
**Used in**: `StreamOfConsciousness`
**Props**:
```typescript
interface AgentActivityItemProps {
  update: BrainUpdate; // The specific update object
}
```

#### `TaskCardStack`
**Description**: Visual representation of tasks in the registry, categorized by status.
**Used in**: `TaskManager`
**Props**:
```typescript
interface TaskCardStackProps {
  tasks: BrainTask[]; // Array of project tasks
}
```

---

### Interactive Components

#### `InteractionHUD`
**Description**: Input area for the user to provide feedback or "inject" new constraints into the brain.
**Used in**: `DashboardOverview`, `StreamOfConsciousness`
**Props**:
```typescript
interface InteractionHUDProps {
  isSending: boolean; // Indicates if an interaction is in flight
}
```
**Actions Required**: `onSendMessage(message: string)`

#### `ControlButtons`
**Description**: Action buttons to pause, resume, or stop the Living Brain engine.
**Used in**: `DashboardOverview`, `AgentController`
**Props**:
```typescript
interface ControlButtonsProps {
  status: BrainStatus;
  disabled?: boolean;
}
```
**Actions Required**: `onPause()`, `onResume()`, `onStop()`

---
*Generated by MyContext CLI - Living Brain Analysis Phase*
